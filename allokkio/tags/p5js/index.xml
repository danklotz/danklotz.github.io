<?xml version="1.0" encoding="utf-8" standalone="yes" ?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>P5js on allokkio</title>
    <link>/tags/p5js/index.xml</link>
    <description>Recent content in P5js on allokkio</description>
    <generator>Hugo -- gohugo.io</generator>
    <language>en-us</language>
    <atom:link href="/tags/p5js/index.xml" rel="self" type="application/rss+xml" />
    
    <item>
      <title>Gaussian Star</title>
      <link>/2017/12/28/testing-p5js/</link>
      <pubDate>Thu, 28 Dec 2017 00:00:00 +0000</pubDate>
      
      <guid>/2017/12/28/testing-p5js/</guid>
      <description>&lt;p&gt;Another little Javascript-animation based on Gaussian ellipses.&lt;/p&gt;

&lt;div id=&#34;sketch-holder&#34; align =&#34;justify&#34;&gt;&lt;/div&gt;

&lt;p&gt;Code:&lt;/p&gt;

&lt;pre&gt;&lt;code class=&#34;language-js&#34;&gt;var c_size = 450
var half_c_size = 0.5*c_size;
var c_pad = c_size - 20
var time = 0.0
var increment = 0.002;
var time_to_update = time + 2*increment;
var increment_to_update = 0.03;
var n = 0
var m = 0

function setup() {
  background(&amp;quot;#DDDCD3&amp;quot;);
  var canvas = createCanvas(c_size, c_size);
  canvas.parent(&#39;sketch-holder&#39;);

  // info:
  textSize(19);
  stroke(221,220,211);
  fill(221,220,211);
  ellipse(half_c_size, half_c_size, c_pad/4, c_pad/4);
  //
  fill(&amp;quot;#EEEDE4&amp;quot;);
  text(&amp;quot;    ~68%  \n of the radii&amp;quot;, c_pad/2-40, c_size/2);
  // decoration:
  // outer circles
    fill(221,220,211,0);
    stroke(221,220,211);
    strokeWeight(3);
    ellipse(half_c_size, half_c_size, c_pad/3, c_pad/3);
    ellipse(half_c_size, half_c_size, c_pad/2, c_pad/2);
    ellipse(half_c_size, half_c_size, c_pad, c_pad);
    // outer ellipses
    strokeWeight(1);
    ellipse(half_c_size, half_c_size, c_pad/3, c_pad/4);
    ellipse(half_c_size, half_c_size, c_pad/4, c_pad/3);
    ellipse(half_c_size, half_c_size, c_pad/2, c_pad/3);
    ellipse(half_c_size, half_c_size, c_pad/3, c_pad/2);
    ellipse(half_c_size, half_c_size, c_pad  , c_pad/2);
    ellipse(half_c_size, half_c_size, c_pad/2, c_pad  );
}

function draw() {
translate(width/2, height/2);
rotate(frameCount / 50.0);
  noStroke();
  time += increment;
  if (time &amp;gt;= time_to_update) {
    n = abs(randomGaussian(1,c_pad/4));
    m = abs(randomGaussian(1,c_pad/4));
    time_to_update += increment_to_update

    fill(30, 144, 255, 20);
    ellipse(0, 0, n, m);
  }
}
&lt;/code&gt;&lt;/pre&gt;

&lt;script src=&#34;https://cdnjs.cloudflare.com/ajax/libs/p5.js/0.5.16/p5.js&#34;&gt;&lt;/script&gt;
&lt;script&gt;
// actual script ---------------------------------------------------------------
var c_size = 450
var half_c_size = 0.5*c_size;
var c_pad = c_size - 20
var time = 0.0
var increment = 0.002;
var time_to_update = time + 2*increment;
var increment_to_update = 0.03;
var n = 0
var m = 0

function setup() {
  background(&#34;#DDDCD3&#34;);
  var canvas = createCanvas(c_size, c_size);
  canvas.parent(&#39;sketch-holder&#39;);

  // info:
  textSize(19);
  stroke(221,220,211);
  fill(221,220,211);
  ellipse(half_c_size, half_c_size, c_pad/4, c_pad/4);
  //
  fill(&#34;#EEEDE4&#34;);
  text(&#34;    ~68%  \n of the radii&#34;, c_pad/2-40, c_size/2);
  // decoration:
  // outer circles
    fill(221,220,211,0);
    stroke(221,220,211);
    strokeWeight(3);
    ellipse(half_c_size, half_c_size, c_pad/3, c_pad/3);
    ellipse(half_c_size, half_c_size, c_pad/2, c_pad/2);
    ellipse(half_c_size, half_c_size, c_pad, c_pad);
    // outer ellipses
    strokeWeight(1);
    ellipse(half_c_size, half_c_size, c_pad/3, c_pad/4);
    ellipse(half_c_size, half_c_size, c_pad/4, c_pad/3);
    ellipse(half_c_size, half_c_size, c_pad/2, c_pad/3);
    ellipse(half_c_size, half_c_size, c_pad/3, c_pad/2);
    ellipse(half_c_size, half_c_size, c_pad  , c_pad/2);
    ellipse(half_c_size, half_c_size, c_pad/2, c_pad  );
}

function draw() {
translate(width/2, height/2);
rotate(frameCount / 50.0);
  noStroke();
  time += increment;
  if (time &gt;= time_to_update) {
    n = abs(randomGaussian(1,c_pad/4));
    m = abs(randomGaussian(1,c_pad/4));
    time_to_update += increment_to_update

    fill(30, 144, 255, 20);
    ellipse(0, 0, n, m);
  }
}
&lt;/script&gt;
</description>
    </item>
    
  </channel>
</rss>